{
  "name": "sanitation-fund-model",
  "version": "0.4.0",
  "private": true,
  "description": "Blended-finance revolving fund model for market-based sanitation in LDCs",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "golden:diff": "node tests/golden.record.js --diff",
    "golden:record": "node tests/golden.record.js",
    "verify": "node tools/verify-findings.js",
    "probe": "node tools/probe.js",
    "serve": "node server.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^10.8.1"
  }
}
